from django.apps import AppConfig


class StudioConfig(AppConfig):
    """Configuration for the studio/creation application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'studio'