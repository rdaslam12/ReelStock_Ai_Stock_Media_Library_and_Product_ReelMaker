from django.apps import AppConfig


class LibraryappConfig(AppConfig):
    """Configuration for the public library application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'libraryapp'